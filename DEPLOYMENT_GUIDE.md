# 🚀 دليل نشر بوت YallaBets VIP على Render.com

## 📋 المتطلبات
- حساب GitHub (مجاني)
- حساب Render.com (مجاني)
- توكن البوت من BotFather

---

## الخطوة 1️⃣: رفع الكود على GitHub

### 1. إنشاء مستودع جديد على GitHub
1. اذهب إلى https://github.com/new
2. اسم المستودع: `yallabets-vip-bot`
3. اختر **Public** أو **Private**
4. **لا تضف** README أو .gitignore أو License
5. اضغط **Create repository**

### 2. رفع الملفات
بعد إنشاء المستودع، ستظهر لك التعليمات. استخدم الطريقة التالية:

```bash
# في جهازك المحلي، قم بتحميل ملفات البوت من المجلد yallabets-bot
# ثم افتح Terminal/Command Prompt في المجلد

# تهيئة Git
git init
git add .
git commit -m "Initial commit - YallaBets VIP Bot"

# ربط المستودع (غيّر USERNAME باسم المستخدم الخاص بك)
git remote add origin https://github.com/USERNAME/yallabets-vip-bot.git
git branch -M main
git push -u origin main
```

**ملاحظة**: ستحتاج إلى تسجيل الدخول بحساب GitHub عند الرفع.

---

## الخطوة 2️⃣: النشر على Render.com

### 1. إنشاء Web Service
1. اذهب إلى https://dashboard.render.com
2. اضغط **New +** → **Web Service**
3. اختر **Build and deploy from a Git repository** → **Next**

### 2. ربط GitHub
1. اضغط **Connect account** لربط حساب GitHub
2. اختر المستودع `yallabets-vip-bot`
3. اضغط **Connect**

### 3. إعدادات الخدمة
املأ الحقول التالية:

| الحقل | القيمة |
|------|--------|
| **Name** | `yallabets-vip-bot` |
| **Region** | اختر الأقرب لك (مثل: Frankfurt) |
| **Branch** | `main` |
| **Runtime** | `Node` |
| **Build Command** | `npm install` |
| **Start Command** | `npm start` |
| **Instance Type** | **Free** |

### 4. إضافة Environment Variables
اضغط **Add Environment Variable** وأضف:

| Key | Value | ملاحظات |
|-----|-------|---------|
| `BOT_TOKEN` | `7xxxxxxx:AAxxxxxxxxx` | توكن البوت من BotFather |
| `VIP_CHANNEL_ID` | `-1003495823265` | معرّف قناة VIP |
| `FREE_CHANNEL` | `@yallabets` | اسم القناة المجانية |
| `SUBSCRIPTION_PRICE` | `20` | السعر بالـ Stars |
| `SUBSCRIPTION_DAYS` | `30` | مدة الاشتراك بالأيام |

### 5. النشر
1. اضغط **Create Web Service**
2. انتظر 2-3 دقائق حتى يكتمل النشر
3. ستظهر رسالة "Live" عندما يصبح البوت جاهزاً

---

## الخطوة 3️⃣: التحقق من عمل البوت

### 1. فحص السجلات (Logs)
1. في صفحة الخدمة على Render، اضغط **Logs**
2. يجب أن ترى: `🤖 YallaBets VIP Bot started successfully!`

### 2. اختبار البوت
1. افتح Telegram وابحث عن `@yallabets_vipbot`
2. أرسل `/start`
3. يجب أن يرد البوت برسالة الترحيب

### 3. اختبار الاشتراك
1. أرسل `/subscribe`
2. يجب أن تظهر فاتورة Telegram Stars
3. **لا تدفع الآن** - هذا مجرد اختبار!

---

## الخطوة 4️⃣: إعدادات القناة VIP

### 1. إضافة البوت كمسؤول في القناة
1. افتح قناة VIP في Telegram
2. اذهب إلى **Channel Info** → **Administrators**
3. اضغط **Add Admin**
4. ابحث عن `@yallabets_vipbot`
5. فعّل الصلاحيات التالية:
   - ✅ **Invite Users via Link**
   - ✅ **Ban Users**
6. اضغط **Save**

### 2. تفعيل Join Requests
1. في إعدادات القناة، اذهب إلى **Channel Type**
2. اختر **Private Channel**
3. فعّل **Approve New Members**

---

## ✅ اختبار نهائي

### اختبار الاشتراك الكامل
1. استخدم حساب Telegram آخر (أو اطلب من صديق)
2. أرسل `/subscribe` للبوت
3. ادفع 20 Stars
4. تحقق من:
   - ✅ ظهور رسالة "تم الاشتراك بنجاح"
   - ✅ إضافة المستخدم لقناة VIP تلقائياً
   - ✅ `/status` يظهر 30 يوم متبقي

---

## 🔧 استكشاف الأخطاء

### البوت لا يرد
- تحقق من Logs في Render
- تأكد من صحة BOT_TOKEN
- تحقق من أن الخدمة في حالة "Live"

### لا يتم إضافة المستخدم للقناة
- تأكد من إضافة البوت كمسؤول في القناة
- تحقق من صحة VIP_CHANNEL_ID
- تأكد من تفعيل "Approve New Members"

### الدفع لا يعمل
- تأكد من أن البوت تم تفعيله لاستقبال المدفوعات من BotFather
- استخدم الأمر `/mybots` → اختر البوت → **Payments** → **Telegram Stars**

---

## 📊 مراقبة البوت

### السجلات (Logs)
- اذهب إلى Dashboard → yallabets-vip-bot → **Logs**
- راقب الأخطاء والرسائل

### قاعدة البيانات
- البوت يحفظ بيانات المستخدمين في `users.db`
- يتم حفظها تلقائياً على Render

### النسخ الاحتياطي
- قاعدة البيانات تُحذف عند إعادة النشر
- لحفظ البيانات، استخدم Render Disk (مدفوع)
- أو انتقل لـ VPS بعد 500 مشترك

---

## 🎯 الخطوات التالية

1. ✅ **اختبر البوت بالكامل**
2. ✅ **ابدأ التسويق** (Reddit, Twitter, Discord)
3. ✅ **انشر التوقعات يومياً**
4. ✅ **راقب عدد المشتركين**
5. ⏳ **عند 500 مشترك**: انتقل لـ VPS ($4/شهر)

---

## 💡 نصائح مهمة

- **Render المجاني** يكفي لـ 500-1000 مشترك
- البوت يعمل 24/7 بدون توقف
- التحديثات: فقط ارفع الكود الجديد على GitHub
- Render سيعيد النشر تلقائياً

---

## 📞 الدعم

إذا واجهت أي مشكلة:
1. تحقق من Logs في Render
2. راجع هذا الدليل
3. تأكد من جميع Environment Variables

**وقت النشر المتوقع**: 10-15 دقيقة

🎉 **مبروك! بوتك جاهز للعمل!**
